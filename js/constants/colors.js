console.log("🎨 colors loaded");

// =====================================================
// CENTRAL COLOR PALETTE (DATA ONLY)
// =====================================================
const COLORS = {

  // =====================================================
  // IONS (AUTHORITATIVE NAMES)
// =====================================================
  sodium:    [255, 215,   0],   // Na⁺ — gold
  potassium: [220,  60,  60],   // K⁺ — soft red

  // =====================================================
  // ELECTRICAL SIGNALS
  // =====================================================
  epsp:    [ 60, 220, 120],
  ipsp:    [220, 120, 120],
  ap:      [ 60, 220, 120],
  vesicle: [185, 150, 255],

  // =====================================================
  // SYNAPTIC STRUCTURE
  // =====================================================
  terminalBouton: [120, 220, 140],
  postSynaptic:   [120, 220, 140],

  // =====================================================
  // NEURON ANATOMY
  // =====================================================
  dendrite: [200, 185, 120],
  soma:     [240, 220, 150],
  axon:     [210, 195, 130],

  // =====================================================
  // MYELIN SYSTEM
  // =====================================================
  myelin:        [240, 240, 225],
  myelinShadow: [210, 210, 195],
  nodeAxon:     [255, 215, 150],

  // =====================================================
  // VASCULATURE
  // =====================================================
  arteryWall:      [165,  70,  60],
  arteryHighlight: [220, 120, 100],

  // =====================================================
  // BLOOD CONTENTS
  // =====================================================
  rbcOxy:   [190,  40,  40],
  rbcDeoxy: [ 60, 100, 200],
  oxygen:   [255, 255, 255],
  water:    [160, 210, 255],
  glucose:  [120, 220, 120],

  // =====================================================
  // BLOOD–BRAIN BARRIER
  // =====================================================
  endothelium: [245, 190, 130],
  astrocyte:   [200, 170, 230],
  pericyte:    [220, 180, 140]
};

// =====================================================
// COLOR ACCESS HELPER (GLOBAL, p5 SAFE)
// =====================================================
function getColor(name, alpha = 255) {
  const c = COLORS[name];

  if (!c) {
    console.warn(`⚠️ Color "${name}" not found in COLORS`);
    return color(255, 255, 255, alpha);
  }

  return color(c[0], c[1], c[2], alpha);
}

// -----------------------------------------------------
// GLOBAL EXPORTS (REQUIRED)
// -----------------------------------------------------
window.COLORS   = COLORS;
window.getColor = getColor;
