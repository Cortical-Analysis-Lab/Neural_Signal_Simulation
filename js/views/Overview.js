function drawOverview(state) {
  push();
  fill(200);
  textAlign(CENTER, CENTER);
  textSize(24);
  text("OVERVIEW VIEW", width / 2, height / 2 - 40);
  textSize(14);
  text("Whole neuron integration (placeholder)", width / 2, height / 2);
  pop();
}
